# Axsumawit source

This directory contains the editable React + TypeScript + Tailwind application. See the [repository README](../README.md) for development, curriculum, and branch-based GitHub Pages deployment instructions.

Use Node.js 24. Run `npm install`, then `npm run dev`. Run `npm run publish:site` to rebuild the committed site at the repository root.
